# 2026-05 Audit

## Repo Inventory

- Type: frontend scrollytelling app
- Stack: Svelte 5, Vite, D3, Scrollama
- Primary runtime source: `src/`
- Primary dataset: `src/data/compute_history.json`
- Authoring docs: `README.md`, `CHANGELOG.md`, the `00_*.md.md` through `04_*.md.md` planning files
- Bootstrap docs added in this pass: `AGENTS.md`, `CURRENT_STATE.md`
- Feature queue added in this pass: `docs/agentic-overhaul/two-prompt-loop-plan.md`
- Validation added in this pass: `scripts/validate_repo.js`
- CI added in this pass: `.github/workflows/validate.yml`
- URL state sync now preserves `step`, `linear`, and `speculative` state for shareable story links, keyboard navigation hands focus from the narrative into the chart, and screen reader live regions announce step and chart state changes.

## Maturity Scores

Scores are subjective, based on inspection of the repo as of 2026-05-19.

| Category | Score | Notes |
| --- | --- | --- |
| Docs | 3/5 | Strong narrative docs and this bootstrap layer exist now, but older planning docs and release notes still contain stale claims. |
| Build | 4/5 | `npm run build` passes after install; build is lightweight and deterministic. |
| Tests | 1/5 | No real test suite yet; validation is structural rather than behavioral. |
| Data / Provenance | 3/5 | Dataset includes source fields and speculative markers, but provenance is still manually curated. |
| CI | 3/5 | A basic push/PR validation workflow exists after this pass. |
| Accessibility / Performance | 3/5 | Better than before: keyboard hooks, keyboard navigation, live announcements, focusable SVG points, ARIA labels, and build-time a11y warnings were addressed; there is still no dedicated audit tooling. |
| Deployment | 3/5 | Vite base path and preview support are present; deploy script exists, but deployment is still manual. |
| Maintainability | 4/5 | The component split is clean, and the new validation/bootstrap docs make the repo easier to continue safely. |

## P0 Backlog

- Resolve the license contradiction across `package.json`, the README, and any canonical license file.
- Keep the validation script passing in CI and local development.
- Preserve data provenance for any future `src/data/compute_history.json` edits.

## P1 Backlog

- Add a minimal smoke test or interaction test if the app behavior changes again.
- Evaluate whether `intersection-observer` can be removed from dependencies.
- Decide whether speculative data should live in the same file or a separate dataset once the repo grows.
- Add a dedicated accessibility audit pass if the interaction surface expands.

## P2 Backlog

- Consider extracting data-generation helpers into a clearly documented script if the dataset becomes generated instead of hand-authored.
- Consider a richer provenance/citation layer only if the project needs public redistribution or editorial review.
- Consider deployment automation only after the release process is stable.

## Do Not Do Yet

- Do not rewrite the app into a new framework.
- Do not replace the current data model with a live API unless the editorial goal changes.
- Do not introduce heavy test tooling before the repo has a concrete regression to catch.
- Do not guess at the license or provenance status.
- Do not expand the visual system before the current narrative and data checks stay green.
